# Vterm CLI Specification

> This needs to be rewritten, here are a few notes:

## Dev

dev server should boot

**Hot Reload**

Hot reload is currently working pretty well. Not every reload rerenders perfectly so this needs to be looked at.

## Build

`vterm build` should build the app and output a bundle ready for deployment to npm.

## Deploy

`vterm deploy` should build and deploy the app to npm.

## Init

`vterm init` should create a new project with the correct minimal structure.